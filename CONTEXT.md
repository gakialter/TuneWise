# TuneWise Domain Context

TuneWise 是面向精密光学装调场景的 AI 调机决策支持比赛原型。本文件只统一项目语言，不描述实现方案，也不代表舜宇真实产线事实。

## 场景与异常

**主动对准工站（AA 工站）**：
比赛原型中执行相机模组光学对准并产生首件 MTF 检测结果的唯一工站。
_Avoid_: 主动对准/聚焦/固化工站、复合工站

**首件**：
在一次原型调机任务中，用于判断当前对准状态是否满足原型控制限的首批检测对象。

**四角 MTF 不对称下降**：
中心 MTF 处于合格或临界合格范围，但左上、右上、左下、右下四角 MTF 出现明显不均衡下降的质量异常。
_Avoid_: 普通失焦、整体 MTF 下降

**原型控制限**：
为模拟数据和离线回放预先定义的判定边界，仅用于比赛原型验收，不是企业真实规格。
_Avoid_: 舜宇控制限、产线规格

## 诊断与决策

**主要根因候选**：
对四角 MTF 不对称下降优先排序的四类原因：Pitch/Roll 倾斜、X/Y 偏心、AA 平台测量波动、夹具基准或设备标定漂移。

**次级根因候选**：
仅在附加条件成立时参与排序的原因；当前仅指中心 MTF 接近下限且四角整体偏低时的 Z 向焦点偏移。

**参数候选**：
系统在安全约束内给出的 1—3 组建议调整量，供人审阅和选择，不等同于设备执行指令。
_Avoid_: 自动写参、控制指令、最优参数

**离线回放**：
使用规则约束的模拟数据展示候选调整前后结果的原型验证过程，不代表真实设备试调或真实良率改善。
_Avoid_: 在线调机、真实产线验证

## 用户与知识状态

**AA 工艺工程师**：
比赛原型中的唯一核心用户，负责审阅诊断证据、确认参数候选和提交候选案例；该称谓不映射舜宇真实岗位设置。
_Avoid_: 调机员、质量人员、知识管理员、系统管理员

**人工确认**：
AA 工艺工程师在查看证据与安全校验结果后，对一组参数候选作出的模拟采用决定，不代表真实设备执行或质量放行。
_Avoid_: 自动审批、设备执行、质量放行

**调机任务记录**：
任务关闭后立即保留、用于审计和复盘的完整过程记录。

**待审核案例**：
由已关闭调机任务提交、尚未获得知识审核资格的候选案例，不参与检索、推荐或参数生成。
_Avoid_: 新增知识、有效案例

**已审核案例**：
比赛原型预置并标记为可复用知识的案例，可以参与相似案例检索和参数候选生成。
_Avoid_: 待审核案例、实时学习结果

**最小审计链**：
对人工确认、结果回放和案例提交保留固定演示身份、时间戳与动作内容的记录。

## 任务闭环

**调机任务**：
承载一次 AA 异常批次从数据导入、诊断、参数候选确认、离线回放到复盘沉淀的完整工作单元。

**任务阶段**：
调机任务在闭环中的唯一当前位置；后续阶段只能在其前置阶段完成后进入。
_Avoid_: 页面步骤、任意跳转状态

**成功主流程**：
比赛现场只使用一个预置异常批次完成的无分支演示脚本，不代表系统缺少保护分支。
_Avoid_: 全部系统行为、唯一测试场景

**确定性配对模拟干预回放**：
在固定数据、规则、模拟器版本和扰动种子下，对同一模拟批次使用原始参数与确认参数进行配对运行的离线模拟干预对照；内部技术实现可称 `Paired Counterfactual Simulation`。
_Avoid_: 真实产线反事实实验、真实因果效果证明、写死结果

**保护分支**：
输入、证据、安全条件或任务状态不满足时阻止主流程继续的系统行为，必须测试但不进入现场成功主流程。

## 数据与坐标

**归一化原型单位**：
比赛模拟器内部统一使用的无量纲单位体系；偏移、角度和 MTF 均不得被表述为真实设备物理量。
_Avoid_: 毫米、微米、角秒、真实设备单位

**Normalized Offset Unit**：
`x_offset`、`y_offset`、`z_offset` 的归一化原型单位。

**Normalized Angular Unit**：
`pitch`、`roll` 的归一化原型单位。

**归一化 MTF**：
范围为 0—1 的原型质量指标，不代表企业真实 MTF 测试值或规格。

**角落坐标系**：
以图像中心为原点，X 轴向右为正、Y 轴向上为正；`LT=(-1,+1)`、`RT=(+1,+1)`、`LB=(-1,-1)`、`RB=(+1,-1)`。

**参数正方向**：
`x_offset`、`y_offset` 的正方向分别与 X、Y 轴一致；正 `pitch` 对 Y 为正的上方角落增加局部焦平面误差，正 `roll` 对 X 为正的右侧角落增加局部焦平面误差，正 `z_offset` 对全部区域增加同向误差。该约定只属于比赛模拟器。

**局部焦平面误差**：
由 `z_offset + pitch_coefficient × y_coordinate + roll_coefficient × x_coordinate` 定义的模拟器中间量，其绝对值增大时对应区域 MTF 单调下降。

**可观测数据**：
诊断系统可从导入 CSV 读取的参数、平台状态和 MTF 测量数据，以及由这些数据计算的派生特征。

**故障真值（FaultTruth）**：
仅存在于独立测试元数据中的批次级主要根因标签，用于生成器验证和自动化评估，业务系统不可读取。
_Avoid_: CSV 标签、诊断输入、案例字段

**主要故障真值（primary_fault_truth）**：
一个异常批次唯一的主要故障模式；MVP 不表达或评估多个主要根因标签。

**干扰项（nuisance disturbance）**：
影响可观测结果但不构成第二个主要故障标签的模拟扰动，例如条件受限的 Z 向偏移。

**外部模拟环境**：
负责生成数据和在人工确认后回放结果的版本化因果模拟器；诊断、检索和参数推荐不得读取其隐藏状态、故障类型或系数。

## 故障模式

**PLANE_TILT**：
由 Pitch/Roll 平面倾斜形成稳定空间不对称 MTF 模式的可调故障。

**XY_DECENTER**：
由 X/Y 偏心形成与偏心方向一致、且与平面倾斜可区分的空间 MTF 下降模式。

**PLATFORM_INSTABILITY**：
主要表现为时间序列方差和重复定位误差增加的平台不稳定故障，只允许输出排查建议；`attempt_count` 不得由模拟器根据该故障直接生成。

**REFERENCE_DRIFT**：
伴随持续标定残差或基准残差的夹具基准或设备标定漂移，只允许输出排查建议。

**Z_DEFOCUS_CONDITIONAL**：
主要造成中心及四角整体下降、仅在中心接近下限且四角整体偏低时参与排序的条件性可调故障。

## 检测、诊断与检索

**AA_CORNER_ASYMMETRY**：
中心 MTF 合格或临界合格，且四角最小值、极差或标准差满足版本化持续性规则的目标异常。
_Avoid_: 单点噪声、中心及四角整体下降

**TARGET_ANOMALY**：
检测结果符合 `AA_CORNER_ASYMMETRY`，允许进入诊断主流程。

**NORMAL**：
检测指标未超过原型控制限，主流程停止。

**NON_TARGET_GLOBAL_DEGRADATION**：
中心及四角整体下降的非目标异常，进入保护分支，不得冒充四角不对称异常。

**INSUFFICIENT_DATA**：
样本数量或必需字段不足，无法完成目标异常判定的检测结果。

**SUFFICIENT_EVIDENCE**：
模型得分、类别间距、兼容规则、字段完整性与冲突检查共同满足版本化阈值的诊断状态，允许继续参数推荐。

**INSUFFICIENT_EVIDENCE**：
诊断证据未满足版本化阈值的状态；可以展示 Top-3 排查顺序，但不得生成可执行参数候选。

**Logit 贡献**：
标准化特征值与当前类别模型系数的乘积，只表示该特征对当前类别 logit 的贡献。
_Avoid_: 概率贡献、因果贡献、SHAP 值、真实故障概率

**结构化 KNN**：
在兼容的 `APPROVED` 案例中，使用批次级可观测特征和版本固定的标准化欧氏距离检索相似案例的方法。

**核心决策链**：
异常判定、根因排序、参数候选、安全校验和案例检索顺序的确定性处理链；大模型、网络 API 和 Agent 不得改变其输出。

## 参数方案与安全

**ParameterDirectionEvidence**：
针对一个具体参数记录当前值、标称值、建议方向、支持特征、支持规则和冲突状态的方向证据；缺失或冲突时该参数不可进入候选。

**ParameterConstraintSnapshot**：
任务创建时固定的产品型号、参数标称值、合法范围、步长、单次最大变化和规则版本只读快照。

**Tick**：
参数安全计算使用的整数步数；MVP v1 中一个 tick 对应 0.05 归一化原型单位。
_Avoid_: 二进制浮点直接比较、静默取整

**ParameterPlanCandidate**：
由当前诊断、方向证据、约束快照和兼容案例生成，并经统一安全校验的一组不可变参数候选。
_Avoid_: 最优参数、全局最优、预测最佳方案

**ParameterSafetyValidator**：
对所有候选统一执行范围、步长、最大变化、标称值方向、参数族和方向证据校验的唯一安全验证器。

**ConfirmedPlan**：
AA 工艺工程师确认后形成、由候选标识、候选哈希、身份、时间和相关版本共同绑定的不可修改参数方案。

**STALE**：
候选或确认方案因上游数据、诊断、参数、快照、规则或内容变化而失效的状态，不得用于回放。

**通过当前证据和安全规则生成的候选方案**：
参数候选在界面和报告中的规范称谓。
_Avoid_: 最优参数、全局最优、预测最佳方案

## 回放与可信声明

**scenario_ref**：
由外部模拟环境解析的不可解释、不可枚举隐藏场景引用；应用侧只保存该不透明引用及其哈希，不保存隐藏场景内容。

**ObservableCanonicalizer**：
按固定字段、排序、精度、空值、编码和序列化规则规范化可观测数据，以比较导入基线与模拟重放基线的版本化组件。

**ReplayResultCanonicalizer**：
在计算结果哈希前固定回放结果字段、精度、指标顺序、数组顺序、状态枚举和序列化格式的版本化组件。

**ReplayEvaluationRuleSnapshot**：
用于判定回放改善、退化和控制限状态的只读评价规则快照，前端不可修改。

**ReplayAttempt**：
一次回放启动请求的审计记录；被拒绝或执行失败的尝试不构成有效 `ReplayResult`。

**ReplayResult**：
通过全部前置校验并完成配对模拟运行后形成的不可修改结果记录；报告只能引用它，不得改写它。

**attempt_count**：
当前任务中已被人工确认并实际执行回放的不同参数方案数量，不是模拟器推测的人工试调次数。

**回放免责声明**：
“规则约束模拟环境中的离线回放结果，不代表真实产线良率改善。”

## 验收与评估

**硬门槛**：
安全、隔离、确定性、完整性、离线运行和可信声明方面必须全部通过的交付条件；任一失败，MVP 不得标记为可交付。

**算法质量门槛**：
异常检测、根因排序和案例检索在冻结测试资产上的比赛原型质量线；未达到时必须如实报告，不得修改盲测标签或降低测试难度。

**冻结盲测集**：
最终评估前锁定集合清单、标签、种子、版本和内容哈希，且不参与训练、调参、案例库构建或演示的批次集合。

**相关案例**：
状态、工站、产品、已审核根因、参数族或排查动作及历史结果均与查询批次兼容的 `APPROVED` 案例。

**NO_RELEVANT_CASE_AVAILABLE**：
案例库中不存在任何相关案例的查询状态；不进入 Recall 分母，但必须单独报告。

**可交付**：
全部硬门槛通过，算法质量结果已按冻结资产如实报告，并完成预设 Windows 环境离线人工验收的 MVP 状态。
